extends Parallax2D


#func _ready():
	#var screen_size = get_viewport_rect().size
	#if $Sprite2D:  # Ensure there's a Sprite2D inside
		#var texture_size = $Sprite2D.texture.get_size()
		#$Sprite2D.scale = screen_size / texture_size
